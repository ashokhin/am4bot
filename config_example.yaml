### General options
#
# Airline Manager URL
url: "https://www.airlinemanager.com/"
# Username for login
username: "your_email@example.com"
# Password for login
password: "YourPasswordHere"
# Logging level (debug, info, warn, error)
log_level: "warn"
# Run browser in headless mode
# ! Not recommended to change this option
# ! on systems without GUI support
chrome_headless: true
# Enable detailed Chrome/Chromium debugging logs
chrome_debug: true
# Launch Chrome via chromedp-undetected (patched navigator.webdriver/CDP
# fingerprints, headless via a real Xvfb display) instead of a plain
# exec-allocator. Leave false for local troubleshooting -- see README.md's
# configuration table.
chrome_stealth: false

### Bot-specific configuration
#
# Percentage of budget to use for each category
budget_percent:
  # Percentage of budget for Fuel
  fuel: 75
  # Percentage of budget for Maintenance
  maintenance: 25
  # Percentage of budget for Marketing
  marketing: 75
# Good price thresholds for resources
good_price:
  # Good price for Fuel (per 1,000 Lbs)
  fuel: 550
  # Good price for CO2 (per 1,000 Quotas)
  co2: 140
# Maximum number of hubs for maintenance ("repair lounge", "buy catering") per run
hubs_maintenance_limit: 3
# Whether to repair lounges in hubs
repair_lounges: true
# Whether to buy catering if missing in hubs
buy_catering_if_missing: true
# Catering duration in hours to set when buying catering
catering_duration_hours: "24"
# Catering amount option to select when buying catering
catering_amount_option: "5000"
# Aircraft wear percentage to trigger maintenance
aircraft_wear_percent: "70"
# Max hours to next A-Check to trigger it
aircraft_max_hours_to_check: 48
# Max aircraft for modifications check
aircraft_modify_limit: 5
# Fuel level percentage to trigger refuel. Even the price isn't good
fuel_critical_percent: 15
# Cron-like schedules for services — at least one entry required. Each is a
# standard 5-field cron expression; the day-of-week field alone covers
# "different start time on weekdays vs. weekends" (a common way to make
# login timing look human), e.g. instead of the single entry below:
#     - "0 8 * * 1-5"   # weekdays: run once at 08:00
#     - "0 10 * * 0,6"  # weekends: run once at 10:00
cron_schedules:
    - "*/10 * * * *"
#
# Upper bound, in seconds, of a random delay applied after each trigger and
# before the run actually starts ("floating start"). Makes login/action
# timestamps less mechanically regular. 0 (default) disables it.
cron_jitter_seconds: 120
# List of services to run
services:
    - "company_stats"
    - "alliance_stats"
    - "claim_rewards"
    - "staff_morale"
    - "hubs"
    - "buy_fuel"
    - "marketing"
    - "ac_maintenance"
    - "depart"
alliance_ids:
    - "1" # Grizzly Group
    - "21" # CODESHARE
    - "44" # Alpha Vikings
# Timeout for full round in seconds
timeout_seconds: 180
# Address to expose Prometheus metrics
prometheus_address: ":9150"
